# Acesso a banco SQL (`database/sql`)

Este tópico consolida os conceitos de consultas parametrizadas, leitura com `Scan` e organização mínima de acesso a dados.

## Observação

O exemplo desta pasta usa uma store em memória para focar em modelagem de acesso.
O exemplo com SQLite real permanece em:

- `database/sqlite/main.go`

## Testar

```bash
go test ./...
```
