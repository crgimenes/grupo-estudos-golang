# Variáveis de ambiente e execução externa

Este tópico cobre `LookupEnv`, configuração e `exec.Command` com captura de saída.

## Executar

```bash
go run .
```

## Testar

```bash
go test ./...
```
